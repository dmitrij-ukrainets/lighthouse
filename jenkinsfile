pipeline{
agent any
stages {
stage ('Performance Tests') {
  steps {
    //deleteDir()
    //checkout scm
    bat 'mkdir .cache\\puppeteer'
    bat 'npm install'
    //bat 'npm install puppeteer'
    bat 'npm run lighthouse'
    bat "rmdir /S /Q .cache"
  }
  post {
    always {
      publishHTML (target: [
        allowMissing: false,
        alwaysLinkToLastBuild: false,
        keepAll: true,
        reportDir: '.',
        reportFiles: 'lighthouse-report.html',
        reportName: "Lighthouse"
      ])
    }
  }
}
}
}
pipeline{
agent any
stages('Performance Tests') {
stage{
  steps {
    deleteDir()
    checkout scm
    //sh 'npm install'
    bat 'npm run lighthouse'
  }
  post {
    always {
      publishHTML (target: [
        allowMissing: false,
        alwaysLinkToLastBuild: false,
        keepAll: true,
        reportDir: '.',
        reportFiles: 'lighthouse-report.html',
        reportName: "Lighthouse"
      ])
    }
  }
}
}
}